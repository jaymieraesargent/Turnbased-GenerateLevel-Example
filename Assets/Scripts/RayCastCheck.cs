using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RayCastCheck : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

/*
    move forward if snap point is infront of us and empty
    turn left
    turn right
    move forward if snap point is infront of us and not empty
        - Iteract
        - Attack melee
    check if not wall...is enemy in block space 2 or 3
 
 
 */
